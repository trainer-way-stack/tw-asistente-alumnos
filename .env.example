# URL del backend de Trainer Way (opcional — normalmente se configura desde la app)
TW_BACKEND_URL=https://tw-asistente-backend-production.up.railway.app

# Token de acceso del closer/alumno (opcional — normalmente se configura desde la app)
TW_ACCESS_TOKEN=twc_...
